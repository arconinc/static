# Shared secret for proxying contact form submissions to the Arc CRM backend.
# Get the value from the Arc app owner and put it in .env.local (never commit .env.local).
CONTACT_FORM_API_KEY=

# Override Arc endpoint for local testing (defaults to production if unset)
ARC_CONTACT_ENDPOINT=
